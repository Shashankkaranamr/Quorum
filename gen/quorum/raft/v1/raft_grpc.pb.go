// Raft consensus wire format and write-ahead log format.
//
// These are the same types in both places on purpose. A log entry that travels
// in an AppendEntries and a log entry that lands in the WAL are encoded
// identically, so there is exactly one serialization to get right and exactly
// one to fuzz.
//
// The consensus core in package raft does NOT import these types. It works with
// plain Go structs, and internal/pbconv translates at the boundary. That is
// what keeps "the core performs no I/O" checkable by reading one import list
// (see raft/purity_test.go).

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: quorum/raft/v1/raft.proto

package raftv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RaftTransport_Stream_FullMethodName = "/quorum.raft.v1.RaftTransport/Stream"
)

// RaftTransportClient is the client API for RaftTransport service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// RaftTransport carries messages between peers.
//
// One long-lived client stream per directed link: node A opens a Stream to
// node B and pushes messages into it, and B opens its own Stream back to A for
// the reverse direction. A directed link therefore maps 1:1 to a stream, so
// injecting a one-way partition is exactly "close this stream and refuse to
// accept a new one from that peer".
//
// Delivery is best effort by design. Dropping, delaying, duplicating or
// reordering a message must never violate safety, only liveness -- and the
// deterministic simulator does all four on purpose to prove it.
type RaftTransportClient interface {
	Stream(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[Message, StreamSummary], error)
}

type raftTransportClient struct {
	cc grpc.ClientConnInterface
}

func NewRaftTransportClient(cc grpc.ClientConnInterface) RaftTransportClient {
	return &raftTransportClient{cc}
}

func (c *raftTransportClient) Stream(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[Message, StreamSummary], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &RaftTransport_ServiceDesc.Streams[0], RaftTransport_Stream_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[Message, StreamSummary]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RaftTransport_StreamClient = grpc.ClientStreamingClient[Message, StreamSummary]

// RaftTransportServer is the server API for RaftTransport service.
// All implementations must embed UnimplementedRaftTransportServer
// for forward compatibility.
//
// RaftTransport carries messages between peers.
//
// One long-lived client stream per directed link: node A opens a Stream to
// node B and pushes messages into it, and B opens its own Stream back to A for
// the reverse direction. A directed link therefore maps 1:1 to a stream, so
// injecting a one-way partition is exactly "close this stream and refuse to
// accept a new one from that peer".
//
// Delivery is best effort by design. Dropping, delaying, duplicating or
// reordering a message must never violate safety, only liveness -- and the
// deterministic simulator does all four on purpose to prove it.
type RaftTransportServer interface {
	Stream(grpc.ClientStreamingServer[Message, StreamSummary]) error
	mustEmbedUnimplementedRaftTransportServer()
}

// UnimplementedRaftTransportServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRaftTransportServer struct{}

func (UnimplementedRaftTransportServer) Stream(grpc.ClientStreamingServer[Message, StreamSummary]) error {
	return status.Error(codes.Unimplemented, "method Stream not implemented")
}
func (UnimplementedRaftTransportServer) mustEmbedUnimplementedRaftTransportServer() {}
func (UnimplementedRaftTransportServer) testEmbeddedByValue()                       {}

// UnsafeRaftTransportServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RaftTransportServer will
// result in compilation errors.
type UnsafeRaftTransportServer interface {
	mustEmbedUnimplementedRaftTransportServer()
}

func RegisterRaftTransportServer(s grpc.ServiceRegistrar, srv RaftTransportServer) {
	// If the following call panics, it indicates UnimplementedRaftTransportServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RaftTransport_ServiceDesc, srv)
}

func _RaftTransport_Stream_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(RaftTransportServer).Stream(&grpc.GenericServerStream[Message, StreamSummary]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RaftTransport_StreamServer = grpc.ClientStreamingServer[Message, StreamSummary]

// RaftTransport_ServiceDesc is the grpc.ServiceDesc for RaftTransport service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RaftTransport_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "quorum.raft.v1.RaftTransport",
	HandlerType: (*RaftTransportServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Stream",
			Handler:       _RaftTransport_Stream_Handler,
			ClientStreams: true,
		},
	},
	Metadata: "quorum/raft/v1/raft.proto",
}
